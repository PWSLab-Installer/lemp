- hosts: localhost
  connection: local
  vars:
    php_version: '7.2'
    php_default_version_debian: '7.2'
  # Roles are imported from ansible
  roles:
    - role: nginx
    - role: mysql
    - role: php